@model IEnumerable<AESManagement.AESDataService.Applicant>

@foreach (var item in Model) {
              
    <p>
    @Html.ActionLink(item.fullName, "Applicant", "Application", new { id = item.id }, null)
    </p>
}

